# 📐 Nodes — Дизайн-документ: Карточка узла

**Дата:** 25 февраля 2026 г.  
**Статус:** Утверждено  
**Версия:** 1.0

---

## 🎯 Концепция

**Карточка узла** — ключевой элемент интерфейса Nodes, отображающий:
1. **Что это за привычка** (название, иконка, тип)
2. **Насколько стабильна** (визуализация выполнения за 5 дней)
3. **Как связана с другими** (коннекторы)
4. **Можно ли отметить сегодня** (кнопка Pulse)

---

## 🎨 Дизайн-система

### Цветовая схема

| Переменная | Светлая тема | Тёмная тема |
|------------|--------------|-------------|
| `--primary` | `oklch(0.5393 0.2713 286.7462)` | `oklch(0.6132 0.2294 291.7437)` |
| `--background` | `oklch(0.994 0 0)` | `oklch(0.2223 0.006 271.1393)` |
| `--accent` | `oklch(0.9393 0.0288 266.368)` | `oklch(0.2795 0.0368 260.031)` |

**Характеристики:**
- **Акцент:** Фиолетовый (нейросеть/киберпанк)
- **Шрифты:** Plus Jakarta Sans (основной), Lora (акценты), IBM Plex Mono (код)
- **Скругления:** 1.4rem (современный вид)
- **Темы:** Тёмная по умолчанию

---

## 📊 Типы узлов

Узлы делятся на три типа по способу отслеживания выполнения:

| Тип | Пример | Отметка |
|-----|--------|---------|
| **Binary** (Да/Нет) | Медитация, зарядка | ✅/❌ |
| **Quantitative** (Количество) | 10 отжиманий, 8 стаканов воды | 7/10 |
| **Time-based** (Время) | Чтение 30 мин, медитация 15 мин | 20/30 мин |

---

## 🧮 Расчёт стабильности

### Формула

```
Стабильность за день = min(фактическое / цель, 1.0) * 100%
```

**Примеры:**

| Тип | Цель | Факт | Расчёт | Стабильность |
|-----|------|------|--------|--------------|
| Binary | — | ✅ | 1.0 | 100% |
| Binary | — | ❌ | 0.0 | 0% |
| Quantitative | 10 раз | 7 раз | 7/10 = 0.7 | 70% |
| Time | 30 мин | 20 мин | 20/30 = 0.67 | 67% |
| Time | 30 мин | 45 мин | min(45/30, 1) = 1.0 | 100% |

### Визуализация (5 дней)

**Окно стабильности:** последние 5 дней

```
День 1: 100% → █ (полный)
День 2: 70%  → ▓ (70% заполнен)
День 3: 40%  → ░ (40% заполнен)
День 4: 0%   → ○ (пустой)
День 5: 100% → █ (полный)

Итог: █▓░○█ = 310% / 500% = 62% общая стабильность
```

**Почему 5 дней:**
- Баланс между достижимостью и значимостью
- Психология формирования привычек (первый спад на 3-5 день)
- Визуально понятно (не перегружено)

---

## 🔗 Коннекторы (связи)

### Типы связей (из БД)

| Тип | Описание | Визуализация |
|-----|----------|--------------|
| `enhances` | Усиливает другую ноду | → (зелёная стрелка) |
| `blocks` | Мешает другой ноде | ╳ (красная линия) |
| `requires` | Требует другую ноду | ← (жёлтая стрелка) |
| `alternative` | Альтернатива | ↔ (синяя двусторонняя) |

### Отображение на карточке

**Показывать первые 2 связи:**
```
🔗 Связи:
[Heart] Вечерний сон →
[Sun] Зарядка →
[+5 ещё]
```

---

## 📐 Структура карточки

```
┌─────────────────────────────────────────┐
│  [Icon]  Название узла            [⋮]   │  ← Header
│  ⏱️ Тип • Частота                       │
├─────────────────────────────────────────┤
│                                         │
│  🔗 Связи:                              │  ← Коннекторы
│  [Icon] Связь 1 →  [Icon] Связь 2 →    │
│                                         │
│  █▓░○█  62%                             │  ← Stability Bar
│                                         │
│  🔥 Стрик: 12 дней  |  Σ 127 раз        │  ← Статистика
│                                         │
├─────────────────────────────────────────┤
│  [ 20 / 30 мин ]  [ Отметить ]          │  ← Pulse Button
└─────────────────────────────────────────┘
```

---

## 🗄️ Структура данных

### Node (таблица)

```typescript
interface Node {
  id: string;
  user_id: string;
  name: string;
  description?: string;
  
  // Тип и цели
  type: 'binary' | 'quantitative' | 'time';
  targetValue?: number;      // для quantitative
  targetDuration?: number;   // для time (в секундах)
  unit?: string;             // 'раз', 'мин', 'км'
  
  // Визуал
  color: string;             // HEX цвет
  icon?: string;             // Lucide icon name
  
  // Частота
  frequency: 'daily' | 'weekly' | 'custom';
  
  created_at: string;
  updated_at: string;
}
```

### Impulse (таблица)

```typescript
interface Impulse {
  id: string;
  node_id: string;
  
  // Фактические значения
  actualValue?: number;      // для quantitative
  actualDuration?: number;   // для time (в секундах)
  
  // Авто-расчёт
  completionPercentage: number; // 0-100
  
  completed_at: string;
  created_at: string;
}
```

### Connection (таблица)

```typescript
interface Connection {
  id: string;
  from_node_id: string;
  to_node_id: string;
  type: 'enhances' | 'blocks' | 'requires' | 'alternative';
  strength: number;          // 1-5
  created_at: string;
}
```

---

## 🧩 Компоненты

### Структура папок

```
src/entities/node/
├── NodeCard.tsx              # Основная карточка
├── NodeHeader.tsx            # Header с иконкой и названием
├── StabilityBar.tsx          # Визуализация стабильности
├── NodeStats.tsx             # Статистика (стрик, всего)
├── NodeConnectors.tsx        # Список коннекторов
└── PulseButton.tsx           # Кнопка выполнения

src/features/node/
├── CreateNodeForm.tsx        # Форма создания
├── EditNodeDialog.tsx        # Редактирование
├── DeleteNodeDialog.tsx      # Удаление
└── NodeConnectionsDialog.tsx # Управление связями
```

### NodeCard Props

```typescript
interface NodeCardProps {
  node: Node;
  impulses: Impulse[];        // за последние 5 дней
  connections: Connection[];  // связанные узлы
  
  // Статистика (вычисляемая)
  stability: number;          // 0-100%
  currentStreak: number;
  bestStreak: number;
  totalCompletions: number;
  isCompletedToday: boolean;
  
  // Callbacks
  onPulse: (impulse: Impulse) => void;
  onEdit: () => void;
  onDelete: () => void;
}
```

---

## 🎯 Форма создания узла

### Поля формы

```typescript
interface CreateNodeForm {
  name: string;
  description?: string;
  
  // Тип узла
  type: 'binary' | 'quantitative' | 'time';
  
  // Для quantitative/time
  targetValue?: number;      // 10 (отжиманий)
  targetDuration?: number;   // 1800 (секунд = 30 мин)
  unit?: string;             // 'раз', 'стаканов', 'минут'
  
  // Визуал
  color: string;             // HEX
  icon?: string;             // Lucide icon name
  
  // Частота
  frequency: 'daily' | 'weekly' | 'custom';
}
```

### UI формы

```tsx
<FormField name="type">
  <Select>
    <SelectItem value="binary">Да/Нет</SelectItem>
    <SelectItem value="quantitative">Количество</SelectItem>
    <SelectItem value="time">Время</SelectItem>
  </Select>
</FormField>

{type === 'quantitative' && (
  <>
    <Input type="number" name="targetValue" placeholder="10" />
    <Input name="unit" placeholder="раз, стаканов, км" />
  </>
)}

{type === 'time' && (
  <Input 
    type="number" 
    name="targetDuration" 
    placeholder="1800 (секунд)" 
  />
)}

<FormField name="color">
  <ColorPicker />
</FormField>

<FormField name="icon">
  <IconPicker library="lucide" />
</FormField>
```

---

## 🎭 Состояния Pulse Button

| Состояние | Вид | Текст |
|-----------|-----|-------|
| **Обычное** | Primary кнопка | "Отметить сегодня" |
| **Наведение** | Glow эффект | "Отметить сегодня" |
| **Нажато** | Анимация импульса | — |
| **Выполнено (binary)** | Secondary, галочка | "✓ Выполнено" |
| **Выполнено (quant/time)** | Progress + кнопка | "7/10 • Добавить ещё" |

---

## 📱 Адаптивность

| Breakpoint | Кол-во карточек |
|------------|-----------------|
| Mobile (< 640px) | 1 колонка |
| Tablet (640-1024px) | 2 колонки |
| Desktop (> 1024px) | 3 колонки |

---

## 🎬 Анимации

| Элемент | Анимация | Триггер |
|---------|----------|---------|
| Pulse Button | Wave от центра | Нажатие |
| Stability Bar | Плавное заполнение | Обновление данных |
| Card | Подъём + тень | Hover |
| Connectors | Пульсация линий | Hover на связи |

---

## 📋 Чек-лист реализации

### Фаза 1 (CRUD)

- [ ] `NodeCard.tsx` — базовая карточка
- [ ] `StabilityBar.tsx` — визуализация 0-100%
- [ ] `PulseButton.tsx` — кнопка выполнения
- [ ] `CreateNodeForm.tsx` — форма создания
- [ ] `EditNodeDialog.tsx` — редактирование
- [ ] `DeleteNodeDialog.tsx` — удаление
- [ ] Сохранение в Supabase
- [ ] Список узлов на HomePage

### Фаза 2 (Визуализация)

- [ ] `NodeConnectionsDialog.tsx` — управление связями
- [ ] `NodeConnectors.tsx` — отображение связей
- [ ] React Flow — граф сети
- [ ] Анимации связей

### Фаза 3 (Аналитика)

- [ ] Подсчёт стриков
- [ ] Best streak
- [ ] Total completions
- [ ] Графики прогресса

---

## 🔗 Связанные документы

- [`plan.md`](./plan.md) — Roadmap проекта
- [`PROGRESS.md`](./PROGRESS.md) — Текущий прогресс
- [`supabase-schema.sql`](./supabase-schema.sql) — Схема БД

---

## 📝 Заметки

- **Без эмодзи** — используем Lucide иконки
- **Без фиксированных категорий** — цветовые метки + коннекторы
- **Стабильность 0-100%** — на основе % выполнения за 5 дней
- **Три типа узлов** — binary, quantitative, time
