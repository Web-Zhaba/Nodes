# Деплой на Vercel

## 📤 Быстрый деплой

### Шаг 1: Push в GitHub

```bash
# Инициализация Git (если ещё не сделан)
git init
git add .
git commit -m "Initial commit: Phase 0 setup"

# Создание репозитория на GitHub и push
git remote add origin https://github.com/your-username/nodes-tracker.git
git branch -M main
git push -u origin main
```

### Шаг 2: Подключение к Vercel

1. Перейди на [vercel.com](https://vercel.com)
2. Войди через GitHub
3. Нажми **"Add New Project"**
4. Выбери репозиторий `nodes-tracker`
5. **Важно**: Укажи Root Directory как `nodes-frontend`
6. Нажми **"Deploy"**

### Шаг 3: Настройка переменных окружения

После первого деплоя:

1. Перейди в **Settings** → **Environment Variables**
2. Добавь переменные:
   - `VITE_SUPABASE_URL` = твой Supabase URL
   - `VITE_SUPABASE_ANON_KEY` = твой anon ключ
3. Нажми **"Save"**
4. Перезапусти деплой: **Deployments** → **"Redeploy"**

---

## 🔧 Ручная настройка (опционально)

### Создание vercel.json

```json
{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "vite",
  "outputDirectory": "dist"
}
```

### Локальный preview production сборки

```bash
npm run build
npm run preview
```

---

## 🌐 Домен

### Бесплатный домен Vercel

Твой проект будет доступен по адресу:
```
https://nodes-tracker-xxx.vercel.app
```

### Кастомный домен (опционально)

1. Купи домен (Namecheap, GoDaddy, etc.)
2. В Vercel: **Settings** → **Domains**
3. Добавь свой домен
4. Настрой DNS записи у регистратора

---

## 🔄 CI/CD

Vercel автоматически деплоит при каждом push в ветку `main`:

```
git push → Vercel build → Auto deploy → Live URL
```

### Preview деплои для pull request

Для каждого PR создаётся preview URL:
```
https://nodes-tracker-git-branch-name.vercel.app
```

---

## 📊 Мониторинг

### Vercel Analytics

Включи в **Settings** → **Analytics**:
- Page Views
- Web Vitals (LCP, FID, CLS)

### Ошибки

Vercel автоматически логирует ошибки сборки и runtime ошибки.

---

## ⚠️ Возможные проблемы

### Ошибка "Build failed"

Проверь логи сборки в Vercel Dashboard. Частые причины:
- TypeScript ошибки → `npm run build` локально
- Неверный путь к проекту → укажи `nodes-frontend`

### Переменные окружения не работают

- Убедись, что имена начинаются с `VITE_`
- Перезапусти деплой после добавления переменных

### 404 при обновлении страницы

Добавь `vercel.json`:

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
```

---

## ✅ Чек-лист деплоя

- [ ] Код в GitHub
- [ ] Проект создан на Vercel
- [ ] Root Directory: `nodes-frontend`
- [ ] Переменные окружения добавлены
- [ ] Первый деплой успешен
- [ ] Сайт открывается по .vercel.app домену
