# Nodes — Setup Instructions

## 🚀 Быстрый старт

### 1. Создание Supabase проекта

1. Перейди на [supabase.com](https://supabase.com)
2. Создай аккаунт (если нет)
3. Нажми **"New Project"**
4. Заполни:
   - **Name**: `nodes-tracker`
   - **Database Password**: сохрани в надёжном месте
   - **Region**: выбери ближайшую к тебе
5. Нажми **"Create new project"** и жди ~2 минуты

### 2. Получение API ключей

1. В проекте перейди в **Settings** (шестерёнка внизу)
2. Кликни **API** в левом меню
3. Скопируй два значения:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon/public key** → `VITE_SUPABASE_ANON_KEY`

### 3. Настройка .env.local

1. Открой файл `.env.local` в корне фронтенда
2. Вставь скопированные значения:

```env
VITE_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 4. Создание таблиц БД

1. В проекте Supabase перейди в **SQL Editor** (иконка документа)
2. Нажми **"New Query"**
3. Вставь SQL из файла `docs/supabase-schema.sql`
4. Нажми **"Run"** (или Ctrl+Enter)

### 5. Запуск локальной разработки

```bash
cd nodes-frontend
npm install
npm run dev
```

Открой http://localhost:5173

---

## 📁 Структура проекта

```
nodes-frontend/
├── src/
│   ├── components/     # UI компоненты (Shadcn + кастомные)
│   ├── entities/       # Бизнес-сущности (Node, Connection, Impulse)
│   ├── features/       # Фичи (create-node, auth, etc.)
│   ├── widgets/        # Композитные компоненты
│   ├── pages/          # Страницы приложения
│   ├── store/          # Zustand stores
│   ├── types/          # TypeScript типы
│   ├── hooks/          # Custom React hooks
│   ├── config/         # Конфигурация
│   ├── lib/            # Утилиты и внешние библиотеки
│   └── App.tsx
├── .env.local          # Переменные окружения (не коммитить!)
├── .env.local.example  # Шаблон для .env.local
└── package.json
```

---

## 🎨 Доступные команды

```bash
# Разработка
npm run dev          # Запуск dev-сервера

# Сборка
npm run build        # Production сборка
npm run preview      # Preview production сборки

# Линтинг
npm run lint         # ESLint проверка
```

---

## 📦 Установленные библиотеки

### Основные
- **React 18** — UI фреймворк
- **TypeScript** — типизация
- **Vite** — сборщик
- **Tailwind CSS** — стилизация
- **Shadcn UI** — компоненты

### Состояние и данные
- **Zustand** — state management
- **@supabase/supabase-js** — клиент Supabase

### Утилиты
- **clsx + tailwind-merge** — работа с классами
- **Zod** — валидация схем
- **React Hook Form** — формы

---

## 🔧 Следующие шаги

1. ✅ Настроить Supabase проект
2. ✅ Создать таблицы БД
3. ✅ Запустить локальную разработку
4. ⏳ Реализовать аутентификацию (Фаза 1)

---

## 🆘 Проблемы и решения

### Ошибка "Failed to fetch" при подключении к Supabase
- Проверь, что URL и ключ правильные в `.env.local`
- Убедись, что проект создан в Supabase
- Проверь, что таблица `nodes` существует

### Ошибка импорта "@"
- Убедись, что `tsconfig.json` содержит `paths`
- В VS Code: нажми `Ctrl+Shift+P` → введи "TypeScript: Restart TS Server"

### Shadcn компоненты не работают
- Проверь, что Tailwind настроен
- Убедись, что `components.json` существует

### Ошибка PostCSS "tailwindcss as a plugin"
- Tailwind v4 требует `@tailwindcss/postcss`
- Установи: `npm install -D @tailwindcss/postcss`
- Обнови `postcss.config.js`:
  ```js
  export default {
    plugins: {
      '@tailwindcss/postcss': {},
      autoprefixer: {},
    },
  }
  ```
